/**
 * Componente que se encarga de mostrar al usuario que la informacion está cargando
 */
import React from 'react'
import '../components/styles/Loading.css'
const Loading = () => (
    <div className="loader">Loading...</div>
)

export default Loading